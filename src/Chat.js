import React from 'react';
import './Chat.css';

function Chat({ id, username, timestamp, read, imageUrl, profilePic }) {
    return (
        <div className="chat">
            
        </div>
    )
}

export default Chat
